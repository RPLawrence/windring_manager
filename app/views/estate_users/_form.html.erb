<%= form_for(estate_user) do |f| %>
  <% if estate_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estate_user.errors.count, "error") %> prohibited this estate_user from being saved:</h2>

      <ul>
      <% estate_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :EstateID %>
    <%= f.number_field :EstateID %>
  </div>

  <div class="field">
    <%= f.label :uuid %>
    <%= f.text_field :uuid %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
