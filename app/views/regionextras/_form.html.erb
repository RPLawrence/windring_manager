<%= form_for(regionextra) do |f| %>
  <% if regionextra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(regionextra.errors.count, "error") %> prohibited this regionextra from being saved:</h2>

      <ul>
      <% regionextra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :RegionID %>
    <%= f.text_field :RegionID %>
  </div>

  <div class="field">
    <%= f.label :Name %>
    <%= f.text_field :Name %>
  </div>

  <div class="field">
    <%= f.label :value %>
    <%= f.text_area :value %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
