<p id="notice"><%= notice %></p>

<h1>Os Groups Principals</h1>

<table>
  <thead>
    <tr>
      <th>Activegroupid</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @os_groups_principals.each do |os_groups_principal| %>
      <tr>
        <td><%= os_groups_principal.ActiveGroupID %></td>
        <td><%= link_to 'Show', os_groups_principal %></td>
        <td><%= link_to 'Edit', edit_os_groups_principal_path(os_groups_principal) %></td>
        <td><%= link_to 'Destroy', os_groups_principal, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Os Groups Principal', new_os_groups_principal_path %>
