<%= form_for(spawn_point) do |f| %>
  <% if spawn_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spawn_point.errors.count, "error") %> prohibited this spawn_point from being saved:</h2>

      <ul>
      <% spawn_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :RegionID %>
    <%= f.text_field :RegionID %>
  </div>

  <div class="field">
    <%= f.label :Yaw %>
    <%= f.text_field :Yaw %>
  </div>

  <div class="field">
    <%= f.label :Pitch %>
    <%= f.text_field :Pitch %>
  </div>

  <div class="field">
    <%= f.label :Distance %>
    <%= f.text_field :Distance %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
