<%= form_for(userdatum) do |f| %>
  <% if userdatum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(userdatum.errors.count, "error") %> prohibited this userdatum from being saved:</h2>

      <ul>
      <% userdatum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :UserId %>
    <%= f.text_field :UserId %>
  </div>

  <div class="field">
    <%= f.label :TagId %>
    <%= f.text_field :TagId %>
  </div>

  <div class="field">
    <%= f.label :DataKey %>
    <%= f.text_field :DataKey %>
  </div>

  <div class="field">
    <%= f.label :DataVal %>
    <%= f.text_field :DataVal %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
