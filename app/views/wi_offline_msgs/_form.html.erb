<%= form_for(wi_offline_msg) do |f| %>
  <% if wi_offline_msg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wi_offline_msg.errors.count, "error") %> prohibited this wi_offline_msg from being saved:</h2>

      <ul>
      <% wi_offline_msg.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :uuid %>
    <%= f.text_field :uuid %>
  </div>

  <div class="field">
    <%= f.label :message %>
    <%= f.text_area :message %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
